using System.Windows.Controls;

namespace PdfEdit.Views;

/// <summary>Image gallery workspace. All behaviour lives in the view model and the attached behaviors.</summary>
public partial class ImageToPdfView : UserControl
{
    public ImageToPdfView() => InitializeComponent();
}
