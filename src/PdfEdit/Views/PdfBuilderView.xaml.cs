using System.Windows.Controls;

namespace PdfEdit.Views;

/// <summary>Merge workspace. All behaviour lives in the view model and the attached behaviors.</summary>
public partial class PdfBuilderView : UserControl
{
    public PdfBuilderView() => InitializeComponent();
}
